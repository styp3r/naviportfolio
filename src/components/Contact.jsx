function Contact(){

    let curlyLeft = "{";
    let curlyRight = "}";

    return (
        <div id = "contactContainer">
            <h1 id = "contactTitle">{curlyLeft} Let's talk! {curlyRight}</h1>
            <div></div>
        </div>
    );
}

export default Contact;